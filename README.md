# Snip — bundle branch

> **Generated output — do not hand-edit.**
>
> Assembled automatically by `scripts/build-bundle.mjs`.
> Edit source on `backend`, `frontend`, or `cli`, then regenerate:
>
>     node scripts/build-bundle.mjs --push
